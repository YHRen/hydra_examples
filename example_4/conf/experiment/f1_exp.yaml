# @package _global_
defaults:
  - override /model: cnn
  - override /dataset: cifar10
  - override /metrics: f1

# hydra:
#   job:
#     name: f1_experiment
#   run:
#     dir: outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
