# @package _global_
defaults:
  - override /model: cnn
  - override /dataset: cifar10
  - override /metrics: 
    - mae
    - mse

# hydra:
#   job:
#     name: mae_experiment
#   run:
#     dir: outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
